# No external dependencies

# Specify Python version compatibility
# This script is compatible with Python 3.7 and above
# Documented in a comment as requirements.txt doesn't directly support Python version constraints.